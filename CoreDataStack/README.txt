README.txt

While doing this research into core data will set up git as I would for production.

git branches:

master (Production)
	hotFix_nn - as required
releaseCandidate
development
	features - as required
futures - where preliminary docs, notes and code snippets for future work are held.